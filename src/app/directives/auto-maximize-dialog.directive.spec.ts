import { AutoMaximizeDialogDirective } from './auto-maximize-dialog.directive';

describe('AutoMaximizeDialogDirective', () => {
  it('should create an instance', () => {
    const directive = new AutoMaximizeDialogDirective();
    expect(directive).toBeTruthy();
  });
});
