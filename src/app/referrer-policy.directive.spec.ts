import { ReferrerPolicyDirective } from './referrer-policy.directive';

describe('ReferrerPolicyDirective', () => {
  it('should create an instance', () => {
    const directive = new ReferrerPolicyDirective();
    expect(directive).toBeTruthy();
  });
});
