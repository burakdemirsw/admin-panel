import { FastTransferPipe } from './fast-transfer.pipe';

describe('FastTransferPipe', () => {
  it('create an instance', () => {
    const pipe = new FastTransferPipe();
    expect(pipe).toBeTruthy();
  });
});
